---
status: accepted
---

# Use rendered Google Search as the search source

Google Search results are retrieved through the managed Surf browser and reduced to structured organic records. Rendered Google ranking and persistent browser context are part of the product, so API search engines are not substituted when Google is blocked; the CLI returns a structured failure instead.
