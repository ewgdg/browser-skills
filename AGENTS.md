# browser-skills — notes for agents

`skills/surf/runtime-revision` pins the runtime the skill installs: the pin names the commit holding the runtime change, so write it in the same change as that edit, which makes the pin update the next commit.

